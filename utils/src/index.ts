/**
 * @neon-cards/utils
 *
 * Utilidades genéricas puras (sin dependencia de Home Assistant ni de
 * LitElement). Si una función necesita `hass` o el DOM, no pertenece aquí.
 */

export const UTILS_PACKAGE_VERSION = '0.1.0';
