/**
 * @neon-cards/shared
 *
 * Componentes visuales, mixins de LitElement y estilos (la estética neón)
 * reutilizados por más de una tarjeta. Cuando una tarjeta necesite algo que
 * ya usa otra, se mueve aquí — nunca se copia (acuerdo nº4).
 */

export const SHARED_PACKAGE_VERSION = '0.1.0';
