# src/utils

🇪🇸 Español (esta sección) · 🇬🇧 [English below](#srcutils-english)

Utilidades genéricas puras: sin dependencia de Home Assistant ni de
LitElement. Si una función necesita `hass` o el DOM, pertenece a `src/ha`
o `src/shared`, no aquí.

---

<a id="srcutils-english"></a>

# src/utils (English)

🇬🇧 English (this section) · 🇪🇸 [Español arriba](#srcutils)

Pure, generic utilities: no dependency on Home Assistant or LitElement.
If a function needs `hass` or the DOM, it belongs in `src/ha` or
`src/shared`, not here.
