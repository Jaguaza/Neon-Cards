/**
 * Punto de entrada único de Neón Cards.
 *
 * Se empaqueta en un solo archivo (dist-cards/neon-cards.js) que registra
 * TODAS las tarjetas. Un único recurso Lovelace para toda la colección,
 * no uno por tarjeta.
 *
 * Añadir una tarjeta nueva = añadir una línea de import aquí. Sin
 * descubrimiento automático de carpetas (acuerdo nº10: nada de magia
 * oculta).
 */
import './cards/entity';
