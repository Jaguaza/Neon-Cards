/**
 * @neon-cards/ha
 *
 * Todo lo que depende directamente de Home Assistant vive aquí: tipos del
 * objeto `hass`, escucha de eventos, helpers de estado de entidades, etc.
 * Las tarjetas consumen este paquete en vez de hablar con Home Assistant
 * directamente, para poder centralizar cambios de compatibilidad (acuerdo
 * nº24).
 */

export const HA_PACKAGE_VERSION = '0.1.0';
