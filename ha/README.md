# @neon-cards/ha

Todo el código que depende directamente de Home Assistant: tipos del objeto
`hass`, eventos, y helpers de estado de entidades.

Las tarjetas nunca hablan con Home Assistant directamente — pasan siempre por
aquí, para poder centralizar el rango de versiones soportadas (acuerdo nº24).
